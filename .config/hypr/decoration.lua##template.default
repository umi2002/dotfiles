hl.config({
    decoration = {
        rounding = 10,
{% if yadm.class == "Laptop" %}
        blur = {
            enabled = false,
        },
{% endif %}
{% if yadm.class == "Desktop" %}
        blur = {
            enabled = true,
        },
{% endif %}
    },
})
